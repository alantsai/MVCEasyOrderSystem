@model System.String
@{
    ViewBag.Title = "ResetPassword";
}

<h2>重設密碼</h2>

<p>請輸入從email取得的Token，並且打上新密碼。如果尚未取得Token，可以到:
    @Html.ActionLink("忘記密碼","ForgotPassword") 來取得。
</p>

<div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        <div>
            @Html.Label("重設Token")
            @Html.TextBox("resetToken", @Model)
        </div>

        <div>
            @Html.Label("新密碼")
            @Html.Password("password")
        </div>
        
        <input type="submit" value="送出" />

    }

</div>
