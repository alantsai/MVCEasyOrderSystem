@using MvcEasyOrderSystem.ViewModels
@model List<Group<string, int>>

<ol>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(string.Format("{0}({1})",@item.Key.ToString(), @item.Value),
        ViewData["Controller"].ToString(), new {Id = item.Id})
        </li>     
    }
</ol>